# Copy to .env (or set env vars) before запуском.
CLICKHOUSE_PASSWORD=clickpass
CLICKHOUSE_DB=sentiment

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Optional: HF model id for ml_service. If download fails, service will start with a dummy model.
MODEL_NAME=cointegrated/rubert-tiny-sentiment-balanced